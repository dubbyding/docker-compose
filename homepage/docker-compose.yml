services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    restart: unless-stopped
    ports:
      - "10.0.0.2:3001:3000"
    environment:
      HOMEPAGE_ALLOWED_HOSTS: home.rajmaharjan.com
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock:ro
